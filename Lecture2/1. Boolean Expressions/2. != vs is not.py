print("!= (Inequality operator)")
a = [1, 2, 3]
b = [1, 2, 3]
print(a != b)  # False, because the values inside the lists are the same

print("is not (Identity inequality operator)")
a = [1, 2, 3]
b = [1, 2, 3]
print(a is not b)  # True, because they are two different objects in memory
