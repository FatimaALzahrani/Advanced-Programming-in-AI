print("== (Equality operator)")

a = [1, 2, 3]
b = [1, 2, 3]
print(a == b)  # True, because the values inside the lists are the same


print("is (Identity operator)")
a = [1, 2, 3]
b = [1, 2, 3]
print(a is b)  # False, because they are two different objects in memory
